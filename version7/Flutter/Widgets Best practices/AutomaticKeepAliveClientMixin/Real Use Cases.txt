Tabs with Stateful Widgets:

2. PageView with form progress
Example: Multi-step form or quiz
[ Step 1 ] → [ Step 2 ] → [ Step 3 ]
Each step is a page in a PageView.

Without keep-alive:
Going back to Step 1 loses the typed input.

With keep-alive:
Input stays as it was — user doesn’t lose progress.